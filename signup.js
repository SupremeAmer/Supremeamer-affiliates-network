// ...useForm logic for email, country, number, name, dob, picture...
const handleSubmit = async (e) => {
  e.preventDefault();
  // collect form data and picture
  const form = new FormData();
  form.append('email', email);
  form.append('country', country);
  form.append('number', number);
  form.append('name', name);
  form.append('dob', dob);
  form.append('picture', pictureFile);

  const resp = await fetch('http://localhost:3000/signup', { method: 'POST', body: form });
  const data = await resp.json();
  if (data.user) {
    localStorage.setItem('supremeamer_user', JSON.stringify(data.user));
    // redirect to main affiliate page
  }
};

















































































































































































